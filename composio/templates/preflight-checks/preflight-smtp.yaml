{{- $disableK8SecretCheck := .Values.global.disableK8SecretCheck | default false }}
{{- if .Values.apollo.smtp.enabled }}
{{- $smtp := dict }}
{{- if not $disableK8SecretCheck }}
{{- $smtpContext := dict "secretRef" .Values.apollo.smtp.secretRef "key" .Values.apollo.smtp.key "namespace" .Release.Namespace }}
{{- $smtp = include "apollo.parseSmtpUrl" $smtpContext | fromJson }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    troubleshoot.sh/kind: preflight
  name: "{{ .Release.Name }}-preflight-smtp"
stringData:
  preflight.yaml: |
    apiVersion: troubleshoot.sh/v1beta2
    kind: Preflight
    metadata:
      name: "{{ .Release.Name }}-smtp-preflight"
    spec:
      {{- if and .Values.apollo.smtp.enabled (not $disableK8SecretCheck) }}
      collectors:
        - secret:
            name: "{{ .Values.apollo.smtp.secretRef }}"
            namespace: "{{ .Release.Namespace }}"
            key: "{{ .Values.apollo.smtp.key }}"
            includeValue: false
        {{- if and (hasKey $smtp "host") (hasKey $smtp "port") }}
        # Optional: basic TCP connectivity to SMTP host:port (does not validate auth)
        - runPod:
            collectorName: "check-smtp-tcp"
            namespace: "{{ .Release.Namespace }}"
            timeout: 60s
            podSpec:
              restartPolicy: Never
              containers:
                - name: check
                  image: alpine:3.19
                  command: ["/bin/sh", "-c"]
                  args:
                    - |
                      apk add --no-cache busybox-extras >/dev/null 2>&1 || true
                      if nc -vz -w 5 "{{ $smtp.host }}" "{{ $smtp.port }}"; then
                        echo "RESULT=OK"
                      else
                        echo "RESULT=ERROR"
                      fi
        {{- end }}
      analyzers:
        - secret:
            checkName: "SMTP password source secret present"
            secretName: "{{ .Values.apollo.smtp.secretRef }}"
            namespace: "{{ .Release.Namespace }}"
            key: "{{ .Values.apollo.smtp.key }}"
            outcomes:
              - warn:
                  message: "Missing SMTP connection string Secret {{ .Values.apollo.smtp.secretRef }} or key {{ .Values.apollo.smtp.key }}. This warning only applies when using Kubernetes Secret-based configuration. If you provide this value via init containers/file-mounted secrets, you can ignore this warning or set global.disableK8SecretCheck=true."
              - pass:
                  message: "Found SMTP connection string Secret."
        {{- if and (hasKey $smtp "host") (hasKey $smtp "port") }}
        - textAnalyze:
            checkName: "SMTP host reachable (TCP)"
            fileName: "/check-smtp-tcp.log"
            regexGroups: 'RESULT=(?P<Result>[A-Z_]+)'
            outcomes:
              - pass:
                  when: 'Result == "OK"'
                  message: "SMTP {{ $smtp.host }}:{{ $smtp.port }} is reachable."
              - fail:
                  message: "Cannot reach SMTP {{ $smtp.host }}:{{ $smtp.port }} from the cluster."
        {{- end }}
      {{- else }}
      collectors: []
      analyzers: []
      {{- end }}
{{- end }}
