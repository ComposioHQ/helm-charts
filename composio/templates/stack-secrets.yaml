{{- $secretName := printf "%s-apollo-admin-token" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
  name: {{ $secretName }}
type: Opaque
stringData:
  APOLLO_ADMIN_TOKEN: {{ include "apollo-admin-token" . }}
{{- end }}
---
{{- $secretName := printf "%s-composio-api-key" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
stringData:
  COMPOSIO_API_KEY: {{ include "composio-api-key" . }}
{{- end }}
---
{{- $secretName := printf "%s-encryption-key" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  ENCRYPTION_KEY: {{ include "encryption-key" . }}
{{- end }}
---
{{- $secretName := printf "%s-jwt-secret" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  JWT_SECRET: {{ include "jwt-secret" . }}
{{- end }}
---
{{- if .Values.openAI.enabled }}
{{- $secretName := printf "%s-openai-credentials" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  OPENAI_API_KEY: {{ include "getSecretCred" (dict "name" .Values.openAI.secretRef "namespace" .Release.Namespace "key" .Values.openAI.key) }}
{{- end }}
{{- end }}
---
{{- if eq .Values.apollo.objectStorage.backend "s3" }}
{{- $secretName := printf "%s-s3-credentials" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  S3_ACCESS_KEY_ID: {{ include "getSecretCred" (dict "name" $.Values.apollo.objectStorage.accessKey.secretName "namespace" .Release.Namespace "key" $.Values.apollo.objectStorage.accessKey.key) }}
  S3_SECRET_ACCESS_KEY: {{ include "getSecretCred" (dict "name" $.Values.apollo.objectStorage.secretKey.secretName "namespace" .Release.Namespace "key" $.Values.apollo.objectStorage.secretKey.key) }}
{{- end }}
{{- end }}
---
{{- if $.Values.apollo.smtp.enabled }}
{{- $secretName := printf "%s-smtp-credentials" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  SMTP_CONNECTION_STRING: "{{ include "getSecretCred" (dict "name" .Values.apollo.smtp.secretRef "namespace" .Release.Namespace "key" .Values.apollo.smtp.key) }}"
{{- end }}
{{- end }}
---
{{- $secretName := printf "%s-temporal-encryption-key" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  TEMPORAL_TRIGGER_ENCRYPTION_KEY: {{ include "temporal-encryption-key" . }}
{{- end }}
---
{{- if eq .Values.apollo.objectStorage.backend "azure_blob_storage" }}
{{- $secretName := printf "%s-azure-connection-string" .Release.Name -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  AZURE_CONNECTION_STRING: {{ include "getSecretCred" (dict "name" $.Values.apollo.objectStorage.azureConnectionString.secretName "namespace" .Release.Namespace "key" $.Values.apollo.objectStorage.azureConnectionString.key) }}
{{- end }}
{{- end }}
---
{{- $secretName := "external-postgres-secret" -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  url: "postgresql://{{ .Values.databaseMigration.user }}:{{ include "getSecretCred" (dict "name" $.Values.databaseMigration.password.secretRef "namespace" .Release.Namespace "key" $.Values.databaseMigration.password.key ) }}@{{ .Values.databaseMigration.host }}:{{ .Values.databaseMigration.port }}/composiodb?sslmode=disable"
  password: {{ include "getSecretCred" (dict "name" $.Values.databaseMigration.password.secretRef "namespace" .Release.Namespace "key" $.Values.databaseMigration.password.key ) }}
{{- end }}
---
{{- $secretName := "external-thermos-postgres-secret" -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  url: "postgresql://{{ .Values.databaseMigration.user }}:{{ include "getSecretCred" (dict "name" $.Values.databaseMigration.password.secretRef "namespace" .Release.Namespace "key" $.Values.databaseMigration.password.key ) }}@{{ .Values.databaseMigration.host }}:{{ .Values.databaseMigration.port }}/thermosdb?sslmode=disable"
{{- end }}
---
{{- if .Values.externalRedis }}
{{- $secretName := "external-redis-secret" -}}
{{- $secret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- if not $secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/managed-by: "Helm"
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/resource-policy: keep
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData:
  url: {{ include "getSecretCred" (dict "name" $.Values.externalRedis.secretRef "namespace" .Release.Namespace "key" $.Values.externalRedis.key ) }}
{{- end }}
{{- end }}
