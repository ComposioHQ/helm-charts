suite: test thermos deployment
templates:
  - thermos/thermos.yaml
tests:
  - it: should create thermos deployment
    documentIndex: 0
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: RELEASE-NAME-thermos
      - equal:
          path: metadata.labels.app
          value: thermos
      - equal:
          path: metadata.labels.release
          value: RELEASE-NAME

  - it: should have correct replica count
    documentIndex: 0
    set:
      thermos.replicaCount: 2
    asserts:
      - equal:
          path: spec.replicas
          value: 2

  - it: should use correct image
    documentIndex: 0
    set:
      thermos.image.repository: test-thermos
      thermos.image.tag: latest
      thermos.image.pullPolicy: Always
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: artifacts.composio.io/proxy/composio-rodent/008971668139.dkr.ecr.us-east-1.amazonaws.com/test-thermos:latest
      - equal:
          path: spec.template.spec.containers[0].imagePullPolicy
          value: Always

  - it: should have correct port configuration
    documentIndex: 0
    asserts:
      - equal:
          path: spec.template.spec.containers[0].ports[0].containerPort
          value: 8180

  - it: should have correct resource configuration
    documentIndex: 0
    set:
      thermos.resources:
        requests:
          memory: "1Gi"
          cpu: "500m"
        limits:
          memory: "2Gi"
          cpu: "1"
    asserts:
      - equal:
          path: spec.template.spec.containers[0].resources.requests.memory
          value: "1Gi"
      - equal:
          path: spec.template.spec.containers[0].resources.limits.cpu
          value: "1"

  - it: should include image pull secrets when configured
    documentIndex: 0
    set:
      global.imagePullSecrets:
        - name: thermos-secret
    asserts:
      - equal:
          path: spec.template.spec.imagePullSecrets[0].name
          value: thermos-secret

  - it: should have rolling update strategy
    documentIndex: 0
    asserts:
      - equal:
          path: spec.strategy.type
          value: RollingUpdate
      - equal:
          path: spec.strategy.rollingUpdate.maxUnavailable
          value: 0
      - equal:
          path: spec.strategy.rollingUpdate.maxSurge
          value: 1

  - it: should wire toolkit registry db url by default
    documentIndex: 0
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: TOOLKIT_REGISTRY_DB_URL
            value: postgresql://registry_reader:registry_reader@RELEASE-NAME-toolkit-registry:5432/thermos?sslmode=disable
      - equal:
          path: spec.template.spec.initContainers[0].name
          value: wait-for-toolkit-registry
      - equal:
          path: spec.template.spec.initContainers[0].image
          value: artifacts.composio.io/proxy/composio-rodent/008971668139.dkr.ecr.us-east-1.amazonaws.com/composio-self-host/thermos-toolkit-registry:r20260311_00

  - it: should append thermos additional init containers after toolkit registry wait
    documentIndex: 0
    set:
      thermos.additionalInitContainers:
        - name: custom-init
          image: busybox
          command: ['sh', '-c', 'echo custom']
    asserts:
      - equal:
          path: spec.template.spec.initContainers[0].name
          value: wait-for-toolkit-registry
      - equal:
          path: spec.template.spec.initContainers[1].name
          value: custom-init

  - it: should skip toolkit registry wiring when explicitly disabled
    documentIndex: 0
    set:
      toolkitRegistry.enabled: false
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: TOOLKIT_REGISTRY_DB_URL
      - isNull:
          path: spec.template.spec.initContainers

---
suite: test thermos service
templates:
  - thermos/thermos.yaml
tests:
  - it: should create thermos service
    documentIndex: 1
    asserts:
      - isKind:
          of: Service
      - equal:
          path: metadata.name
          value: RELEASE-NAME-thermos
      - equal:
          path: metadata.labels.app
          value: thermos

  - it: should have correct service configuration
    documentIndex: 1
    set:
      thermos.service.type: ClusterIP
      thermos.service.port: 8180
    asserts:
      - equal:
          path: spec.type
          value: ClusterIP
      - equal:
          path: spec.ports[0].port
          value: 8180
      - equal:
          path: spec.ports[0].targetPort
          value: 8180

---
suite: test toolkit registry resources
templates:
  - thermos/toolkit-registry.yaml
tests:
  - it: should render toolkit registry deployment and service by default
    asserts:
      - hasDocuments:
          count: 2
      - isKind:
          of: Deployment
        documentIndex: 0
      - equal:
          path: metadata.name
          value: RELEASE-NAME-toolkit-registry
        documentIndex: 0
      - equal:
          path: spec.template.spec.containers[0].image
          value: artifacts.composio.io/proxy/composio-rodent/008971668139.dkr.ecr.us-east-1.amazonaws.com/composio-self-host/thermos-toolkit-registry:r20260311_00
        documentIndex: 0
      - isKind:
          of: Service
        documentIndex: 1
      - equal:
          path: spec.ports[0].port
          value: 5432
        documentIndex: 1

  - it: should allow toolkit registry init containers and env
    documentIndex: 0
    set:
      toolkitRegistry.initContainers:
        - name: prep
          image: busybox
          command: ['sh', '-c', 'echo prep']
      toolkitRegistry.env:
        - name: PGAPPNAME
          value: toolkit-registry
    asserts:
      - equal:
          path: spec.template.spec.initContainers[0].name
          value: prep
      - equal:
          path: spec.template.spec.containers[0].env[0].name
          value: PGAPPNAME
      - equal:
          path: spec.template.spec.containers[0].env[0].value
          value: toolkit-registry

  - it: should not render toolkit registry when disabled
    set:
      toolkitRegistry.enabled: false
    asserts:
      - hasDocuments:
          count: 0
